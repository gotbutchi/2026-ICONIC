# M02: Frontend Scaffolding Tasks

- [x] Initialize Vite React project (`create-vite`).
- [x] Install Core Dependencies (`recharts`, `tailwindcss`, `papaparse`).
- [x] Configure Tailwind CSS v4 in `tailwind.config.js` and `index.css`.
- [x] Resolve NPM `EACCES` permission issues.
- [x] Scaffold empty directory structure (`src/components`, `src/data`).
